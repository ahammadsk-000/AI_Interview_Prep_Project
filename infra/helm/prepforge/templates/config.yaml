apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prepforge.fullname" . }}-config
  labels:
    {{- include "prepforge.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "prepforge.fullname" . }}-secrets
  labels:
    {{- include "prepforge.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
